[project]
name = "chatserver"
version = "0.1.0"
description = "FastAPI chat server example"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.115.12",
    "openai-agents>=0.0.14",
]
